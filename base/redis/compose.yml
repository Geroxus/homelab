services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: unless-stopped
    ports:
      # exposed s.t. I can inspect this via tools
      - 6379:6379
    networks:
      - edge

